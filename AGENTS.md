# 项目开发约定

本文件适用于整个仓库。开始修改前，先阅读 `docs/architecture.md` 和 `CONTRIBUTING.md`，遵循现有职责划分。

## 后续新功能必须分类

- 延续当前项目架构，不把新功能堆回 `ui.py`、`engine.py` 或主窗口文件。
- 独立功能放 `fishing_assistant/features/<功能名>/`，按需要拆为 `model.py`（规则和状态）、`recognition.py`（识别）、`runner.py`（执行）、`page.py`（界面）。复用已有模块；不为凑目录创建空文件。
- 通用页面布局放 `desktop/pages/`，交互和配置联动放 `desktop/controllers/`，复用控件、弹窗和主题分别放对应模块。
- 操作页保持简洁：参数解释放悬停提示，完整教程放使用说明，补充步骤按需展开；危险告知和完整错误原因不得隐藏。具体遵循 `docs/ui-copy.md`。
- 自动化执行流程放 `automation/`；通用图片识别放 `vision/`；配置定义、默认值和校验统一放 `config.py`。
- 协调器只负责组装、共享状态和生命周期。旧兼容入口只做导出，不新增业务逻辑。
- 文件或类开始承担多个独立职责时，按职责继续拆分；不要仅按行数切割，也不要创建重复实现或通用杂物模块。

## 后续开发以 OK 优先

- 新功能、问题修复和识别优化，优先使用 OK 框架的识别、截图与窗口交互能力，并复用项目已有封装。
- 定位优先采用 OK 特征/锚点与实际客户区几何信息，适配分辨率、长宽比及 DPI；不把单台设备的分辨率或截图点击坐标写成运行前提。
- 默认方案保持 OK。旧像素识别仅作为用户主动选择的兼容模式保留，不因 OK 识别失败而自动回退，不擅自改变已保存的用户选择。
- OK 能力不足时，先说明限制和验证结果；新增非 OK 兼容方案需先征得用户确认。既有明确批准的辅助判断和兼容行为保持兼容，不在无关任务中替换。

## 修改时保持边界

- 界面不直接发送游戏按键；复用现有任务互斥、停止检查和窗口后端。
- 识别以 OK 框架为基础，保留用户明确选择的兼容方案，不自动切换识别模式。
- 现有内部 mixin 不单独实例化，不增加构造函数或隐式覆盖其他分块的同名方法。
- 保留原有入口、配置兼容性和安全确认。结构重构与算法、阈值或操作时序的变更分开处理。

## 完成要求

- 新功能同步补充测试和使用说明；目录或职责变化时更新 `docs/architecture.md` 的导航。
- 界面检查日间和夜间主题，复用防滚轮误改控件。
- 运行相关测试和完整回归，说明跳过项目及未进行的实机验证，不删除断言来掩盖失败。
- 未经本次任务明确要求，不改版本号、不打包发布，也不启用预备远程功能。
